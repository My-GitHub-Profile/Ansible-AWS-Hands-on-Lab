--------------------------------------------------------------------------------------------------------------------------------------------------------

ansible all -a "df -h"
--------------------------------------------------------------------------------------------------------------------------------------------------------
This command checks the available and used space on a specific filesystem:

ansible all -m shell -a "df -h /dev/sda"

--------------------------------------------------------------------------------------------------------------------------------------------------------
ansible all -a "free -m"    (-m flag: displays data in megabytes)
